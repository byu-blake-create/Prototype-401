# Database Configuration
DB_USER=postgres
DB_HOST=localhost
DB_NAME=gylt
DB_PASSWORD=your_password_here
DB_PORT=5432

# Server Configuration
PORT=5000

# Instructions:
# 1. Copy this file to .env in this directory
# 2. Update the values with your actual PostgreSQL credentials
# 3. Make sure PostgreSQL is running and the 'gylt' database exists
# 4. Run the schema.sql and seed.sql scripts from the db/ folder to set up the database
